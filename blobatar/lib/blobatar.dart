/// Deprecated: import 'package:flutter_blobatar/flutter_blobatar.dart' instead.
///
/// Kept as a shim so existing code using `package:blobatar/blobatar.dart`
/// keeps working during the rename to `flutter_blobatar` (the `blobatar`
/// name is taken on pub.dev by the official SDK).
library;

export 'flutter_blobatar.dart';
